services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: owey
      POSTGRES_PASSWORD: owey
      POSTGRES_DB: owey
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: